// for upload dialog in teacher resource,
export const
    Dialog_Success = (to) => to + 'Upload_Dialog_Success',

    // for changing data in form
    CHANGE_RESOURCE_CODE = (to) => to+'_CHANGE_RESOURCE_CODE',
    CHANGE_CLASS_CODE = (to) => to+'_CHANGE_CLASS_CODE',
    CHANGE_DEADLINE = (to) => to+'_CHANGE_DEADLINE',
    CHANGE_SEMESTER = (to) => to+'_CHANGE_SEMESTER',
    CHANGE_QUARTER = (to) => to+'_CHANGE_QUARTER',
    CHANGE_LOW_GRADE = (to) => to+'_CHANGE_LOW_GRADE',
    CHANGE_HIGH_GRADE = (to) => to+'_CHANGE_HIGH_GRADE',
    CHANGE_DESCRIPTION = (to) => to+'_CHANGE_DESCRIPTION'
